'use strict';

var TelegramBot = require('node-telegram-bot-api');

module.exports = function (redis, ks, logger) {
  var redisPrefix       = 'telegram:';

  var TelegramController = {};

  return TelegramController;
};